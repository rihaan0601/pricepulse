services:
  - type: web
    name: pricepulse-backend
    env: docker
    dockerfilePath: ./harvester/Dockerfile
    plan: free
    region: singapore
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: pricepulse-db
          property: connectionString

databases:
  - name: pricepulse-db
    plan: free
    region: singapore
    databaseName: pricepulse_db
    databaseUser: pricepulse_user
